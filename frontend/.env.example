# API Backend URL - Development
VITE_API_URL=http://localhost:8080

# For production (GCP Cloud Run)
# After deploying backend with Terraform, use the backend_url output:
# VITE_API_URL=<backend-url-from-terraform-output>touch variables.tf