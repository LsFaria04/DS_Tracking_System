# =============================================================================
# FOR CLOUD RUN + CLOUD SQL (GCP Production)
# =============================================================================
# Use these values for Cloud Run deployment
# After running `terraform apply`, use the output values:
#
# DB_HOST=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
# Example: DB_HOST=/cloudsql/madeinportugal:europe-west1:tracking-db
#
# DB_PORT=5432
# DB_USER=tracking_user
# DB_PASSWORD=your_secure_password_here
# DB_NAME=tracking_db
#
# Note: Cloud Run will inject these via Terraform, so you may not need .env for production
# The .env file is mainly for local development

# =============================================================================
# CURRENT CONFIGURATION (Update based on your deployment target)
# =============================================================================

DB_HOST=postgres
DB_PORT=5432
DB_USER=tracking_user
DB_PASS=change_me_secure_password
DB_NAME=tracking_db
